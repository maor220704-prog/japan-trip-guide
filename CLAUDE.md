# CLAUDE.md - מדריך יפן

הנחיות לכל סוכן שעובד על הפרויקט הזה בעתיד.

## מה זה
מדריך טיול PWA בעברית/RTL לטיול סולו ליפן, דוגמת "אח" (sibling app) ל-`מידע למטייל` (הפרויקט ב-`../אפליקציה למזרח`). נבנה מאותה ארכיטקטורה, אך **לא** מאותה מערכת צבעים - ראו "פלטת הצבעים" למטה.

**הערה חשובה למי שקורא מסמך זה:** זהו מסמך שנכתב בשלב הסקאפולד הראשוני (לפני שינוי הצבעים והתמונות) ומתוחזק מאז. תמיד תבדקו את ה-git log וה-`src/index.css`/`src/data/cities/*.ts` בפועל לפני שסומכים על תיאור כאן - במיוחד סעיפי "מה שונה מהמקור" ו"פלטת הצבעים".

**מסלול:** אוסקה ← קיוטו ← פוג'י (אזור חמשת האגמים) ← האקונה ← טוקיו. יש עכשיו תאריכים אמיתיים לכל עיר - `src/data/hotels.ts` הוא מקור האמת (תאריכי צ'ק אין/אאוט אמיתיים מההזמנות), ומוצגים גם במסך הבית, גם בכותרת כל עמוד עיר, וגם ברשימת הציוד. תוכן תלוי-עונה (אקלים ברשימת הציוד) עדיין נשאר כללי ("סוף ספטמבר עד אמצע אוקטובר") כי הוא נחקר לפני שהתאריכים המדויקים היו ידועים - לא לטעון דיוק יומי שהמחקר לא נתן.

## ערימת טכנולוגיה (זהה למקור)
React 19 + TypeScript + Vite 8 + Tailwind v4, ללא בקאנד - סטטי לגמרי, PWA עם `vite-plugin-pwa` (מצב `generateSW`), פרוס על GitHub Pages תחת חשבון `maor220704-prog`, ריפו `japan-trip-guide`.

- **URL חי:** https://maor220704-prog.github.io/japan-trip-guide/
- **base path:** `/japan-trip-guide/` - חייב להיות זהה במדויק בארבעה מקומות: `vite.config.ts` (`base` + PWA manifest `start_url`/`scope` + `navigateFallback`), `src/App.tsx` (`basename={import.meta.env.BASE_URL}`), `public/404.html` (`pathSegmentsToKeep`), ו-`index.html` (script הפענוח התואם). **באג אמיתי קרה פעם באפליקציית המקור מהחמצת מקום אחד - לבדוק את כל הארבעה יחד בכל שינוי לשם הריפו.**

## מה הועתק כמו שהוא מהאפליקציה המקורית
- `src/index.css` - כל בלוק `@theme`, גופן Assistant self-hosted, כל מחלקות ה-CSS המותאמות אישית (`.nav-glass`, `.header-elevated`, `.tab-chip-active`, `.glow-halo` + `--selected`, `.packing-check` + `--checked`, `.packing-card` + `--pulse`, `.packing-panel`).
- `src/components/Icon.tsx` + `iconPaths.ts` - מערכת אייקוני SVG מוטבעים (ללא תלות ברשת). לא נוספו אייקונים חדשים - הסט הקיים כיסה את כל הצרכים.
- `src/components/NavBar.tsx`, `ErrorBoundary.tsx`, `src/pages/NotFound.tsx`, `src/pages/PackingList.tsx`, `src/components/Loader.tsx` - מבנה זהה, טקסטים/routes מותאמים.
- `vite.config.ts`, `.github/workflows/deploy.yml`, `package.json` (scripts/deps) - תבנית זהה.

## מה שונה מהמקור (במכוון)
- **מדינה אחת, לא שתיים** - אין `Country` type, אין טאב מדינה ב-`CityList`. חמש ערים בסדר מסלול קבוע (`CITY_IDS`).
- **שפה אחת (יפנית), לא שתיים** - `PhraseLanguage` הוא `"japanese"` בלבד. `Dictionary.tsx` לא כולל מתג שפה (אין צורך), רק רשימת ביטויים כלליים + ניב אזורי לפי עיר (קנסאי-בן באוסקה/קיוטו, אם תועד).
- **קטגוריה אחת פחות** - `Category` הוא `restaurant | attraction | scenic | market | special` (בלי `tailor` - לא רלוונטי למסלול הזה).
- **יש תמונות אמיתיות** (בשונה מהתיאור המקורי של הסשן הראשון) - 53 תמונות מ-Unsplash/Wikimedia (רישיון חופשי, מאומת פר-תמונה), כולל מסעדות (לא רק אטרקציות כמו באפליקציית המקור). `src/components/CityPhotoGallery.tsx` מציג גלריה נגללת בראש כל עמוד עיר - בונה אותה מ-`city.image` + עד 5 תמונות מקומות (עדיפות לאטרקציות/נופים), עם דה-דופ לפי `src` בלבד (**שים לב**: זה לא תופס מקרה שבו שני קבצים שונים מכילים את אותה תמונת מקור בפועל - זה קרה בפועל בקיוטו, ראו lessons.md). לא כל מקום קיבל תמונה - עסקים קטנים בלי כיסוי חופשי דולגו בכנות, ראו `docs/IMAGE-CREDITS.md`.
- **אין קואורדינטות בשום מקום** - לעולם לא להמציא `coords`. כפתור הניווט נבנה מ-`nameLatin` + שם עיר לועזי (`src/data/cityLatinNames.ts`).

## פלטת הצבעים
בשונה מהאפליקציה המקורית (ירוק/קרם) - כאן סגול. `src/index.css` (`@theme`): `--color-primary: #8e2de2` (סגול ניאון - שמור לרגעים קטנים ובולטים: צ'קבוקס מסומן, כותרות, מסגרת כרטיס, זוהר), `--color-secondary-container: #ede6fa` (לבנדר בהיר - הטון הדומיננטי בכל מקום שיש טקסט), `--color-tertiary: #b8791f` (זהב חם - הצבע השלישי במכוון לא סגול נוסף, כדי שהפלטה לא תהיה חד-גונית). לעולם לא רקע ניאון מלא מאחורי טקסט רץ - זה פוגע בקריאות ומטשטש את ההבחנה מהצ'קבוקס המסומן.

## עבודה עם סוכני רקע (Agent tool)
כשמאצילים משימה גדולה לסוכן ראשי שמתאם סוכני-משנה משלו (למשל מחקר מקביל לכמה ערים) - לא לסמוך על `SendMessage` בין הסוכנים כדי להעביר תוצאות בחזרה לסוכן הראשי. זה נכשל בפועל (כתובת `"claude"` לא נפתרה, הסוכן הראשי חיכה שעות בלי תוצאה). עדיף: הסוכן הראשי קולט את התוצאה ישירות מקריאת ה-Agent שלו (לא מבקש repost מאוחר יותר), או שהתוצאות נכתבות לקובץ שהסוכן הראשי קורא.

## איפה התוכן
- `src/data/cities/{osaka,kyoto,fuji,hakone,tokyo}.ts` - תוכן לכל עיר (מקומות, אזהרות, ניב מקומי). נכתב ע"י מחקר real עם מקורות מאומתים (`sources` הוא שדה חובה בטיפוס - רשומה בלי מקור לא מתקמפלת).
- `src/data/packingList.ts` - רשימת ציוד: בסיס לכל הטיול + סעיף לכל עיר, מבוסס על מחקר אקלים אמיתי לכל אזור (פוג'י/האקונה קרים ורטובים יותר מאוסקה/קיוטו/טוקיו בגלל הגובה - זה מוצג בפירוש).
- `src/data/phrases.ts` - ביטויי הישרדות ביפנית (רומאג'י), ללא אימוג'י, ללא המצאות.
- `src/data/hotels.ts` - **מקור האמת לתאריכים האמיתיים של המסלול** (שם מלון, כתובת, טלפון, תאריכי צ'ק אין/אאוט, קישור הזמנה). כתובת/טלפון נשלפו בחיפוש אמיתי (לא מהמשתמש) - הניווט בעמוד המלונות נבנה מהטלפון, לא מהכתובת, כי ביפן ניווט נעשה בעיקר לפי מספר טלפון של היעד.
- `src/pages/Hotels.tsx` - עמוד המלונות, כפתור רביעי בסרגל התחתון.

## זיכרון הפרויקט
- `status.md` - איפה עצרנו
- `lessons.md` - טעויות שתוקנו
- `examples.md` - דוגמאות לתוצאה טובה

## סדר קריאה
- בתחילת כל שיחה: `status.md` קודם
- שאר הקבצים - לפי הצורך

## איסור המצאה - אותם כללים כמו במקור
1. כל מקום חייב `sources` לא ריק. עסקים שיכולים להיסגר (מסעדות/קפה) - שני מקורות. מקדשים/פארקים/אתרים היסטוריים - מקור אחד מספיק.
2. לעולם לא להמציא `coords`.
3. "לא הצלחתי לאמת" זו תשובה לגיטימית - מוחקים ולא ממציאים כדי למלא מכסה.
4. אין קלישאת "פריחת סתיו" (koyo) בתוכן אלא אם נבדק שהתאריך המדויק תואם - ברוב יפן ה-koyo השיא הוא נובמבר, אחרי החלון של הטיול הזה (סוף ספטמבר-אמצע אוקטובר). אל תרמז על צביעת עלים בתקופה הזאת בלי בדיקה.
5. אין אימוג'י בשום תוכן.

## פקודות
```
npm install
npm run dev       # http://localhost:5173/japan-trip-guide/
npm run build     # tsc -b && vite build
npm run lint      # oxlint
npm run preview
```

## פריסה
`git push` ל-`master` מפעיל את ה-workflow (`.github/workflows/deploy.yml`) ומפרסם אוטומטית ל-GitHub Pages. Pages מופעל עם `build_type=workflow` (לא מ-branch ישיר).
