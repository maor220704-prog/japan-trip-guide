# design.md - מדריך יפן

מסמך עיצוב קצר. מערכת העיצוב **מועתקת במלואה** מ-`מידע למטייל` (ראו `../אפליקציה למזרח/src/index.css`) - אין כאן שינוי טעם, רק תיעוד של מה שכבר קיים ולמה.

## 1. שפה ו-RTL [חובה]
עברית, `dir="rtl"` בכל האפליקציה. כל שם מקום מוצג בעברית (`name`), עם השם הלועזי המקורי כשורה שנייה קטנה יותר ב-`dir="ltr"` (`nameLatin`) - כדי שאפשר יהיה להראות אותו לנהג מונית/מקומי או להעתיק לניווט.

## 2. פלטת צבעים [חובה - זהה למקור]
טוקנים ב-`@theme` ב-`src/index.css`: `--color-primary` #47664b (ירוק שמיר), `--color-primary-container` #86a789, `--color-tertiary` #9d422c (חימר-אדמדם לאזהרות/הדגשות), רקע חם קרם-לבן (`--color-background` #faf9f4) עם גרדיאנט עדין לכיוון `--color-secondary-container`. **לא שונה עבור יפן** - זו אותה "מערכת עיצוב שכבר עובדת מצוין" שהמשתמש ביקש לשמר.

## 3. גופן [חובה - זהה למקור]
Assistant, self-hosted (`src/assets/fonts/*.woff2`), משקלים 400-700, שני subsets (עברית + לטינית). אין תלות ברשת - עובד אופליין.

## 4. רכיבי UI ייחודיים (מועתקים כמו שהם)
- **`.glow-halo`** - הילת הדגשה על כפתורי ניווט/מידע ב-PlaceCard ועל טאב הפעיל. Hover flourish רק מאחורי `@media (hover: hover) and (pointer: fine)`, `:active` לפידבק לחיצה בכל מכשיר, `:focus-visible` outline נקי בלבד.
- **`.nav-glass`** - זכוכית חצי-שקופה לסרגל התחתון, עם fallback אטום מלא ב-`@supports` לדפדפן בלי backdrop-filter.
- **`.header-elevated`** - כותרת עליונה אטומה עם צל + hairline, לדפים עם תוכן גולל מתחתיה (במקום זכוכית - כי יש שם רק טקסט/רקע אחיד, אין תמונות גוללות).
- **`.tab-chip-active`** - גרדיאנט בהיר לטאב הפעיל בדף עיר.
- **`.packing-check` / `.packing-card` / `.packing-panel`** - צ'קבוקס "ג'לי" מתנפח, כרטיס ציוד עם מסגרת גרדיאנט, פאנל זכוכית בהיר מאוד - כל השלושה בעיצוב זהה לחלוטין למקור.

## 5. פריסה [פתוח - נגזר מהמקור]
- ניווט תחתון קבוע (3 טאבים: מסלול / ציוד / מילון), מוסתר בדף עיר בודד (יש שם טאבים עליונים במקום).
- Bottom padding (`pb-24`) בכל דף עם ניווט תחתון, כדי שתוכן לא ייחסם.
- רוחב מקסימלי `max-w-lg` לניווט התחתון - נשאר קריא גם על מסך רחב.

## 6. מה שונה מהמקור (במכוון) [חובה]
- **אין טאב מדינה** בדף הבית - מדינה אחת (יפן), חמש ערים בשורה אחת לפי סדר מסלול, עם מספר סידורי (1-5) ליד כל שם עיר כדי להדגיש את סדר המסלול.
- **אין מתג שפה** בדף המילון - שפה אחת (יפנית) לא מצדיקה UI של בחירה.
- **עדכון (30/08/2026): תמונות נוספו בפועל** - `city.image` ו-`place.image` מלאים עבור רוב הערים (ראו `docs/IMAGE-CREDITS.md`), באותו פייפליין Unsplash/Wikimedia חופשי-מסחרי כמו הפרויקט המקור, ומיובאים כמודול Vite (`import img from "../../assets/images/...webp"`) לצורך cache-busting. בניגוד למקור, כאן יש תמונות גם למסעדות/בתי קפה, לא רק לאטרקציות - לפי בקשה מפורשת.
- **גלריית תמונות עירונית - פיצ'ר חדש, לא קיים במקור.** `CityHeroCarousel` (קרוסלת רקע מתחלפת מעל הכותרת, שנבנתה מראש כ"חריג" בציפייה לתמונות עתידיות) הוחלף ב-`CityPhotoGallery` - רצועת תמונות אופקית וגלילה חופשית (`overflow-x-auto` + `scroll-snap`, בלי ספריית קרוסלה), ממוקמת מתחת לכותרת הדביקה (כולל הטאבים) ומעל תוכן הטאב הפעיל, כך שהיא נשארת גלויה בראש הדף בכל טאב שנבחר. מציגה עד 6 תמונות: תמונת העיר קודם, ואז עדיפות לתמונות אטרקציה/נוף (המקומות המרכזיים), לא כל תמונה שקיימת בעיר.

## 7. אייקונים [חובה - זהה למקור]
SVG מוטבעים בלבד (`Icon.tsx` + `iconPaths.ts`), Material Symbols Outlined, ללא תלות בגופן אייקונים מרושת. הסט הקיים (explore/luggage/translate/map/info/restaurant/tour/storefront/auto_awesome/warning/gpp_maybe/arrow_forward/error/local_fire_department) כיסה את כל הצרכים - לא נוספו אייקונים חדשים.

## 8. איפה יש חופש [פתוח]
טקסט התוכן עצמו (תיאורים, פסקאות מידע כללי) - כתוב מחדש לגמרי ליפן, לא מתורגם מהמקור. מבנה הנתונים (טיפוסים, תבנית קובץ) כן שוכפל במדויק כדי לשמור על אותה משמעת אנטי-המצאה שהוכחה בפרויקט המקור.
