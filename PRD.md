# PRD.md - מדריך יפן

## 1. מטרה
מדריך טיול אישי, עובד גם בלי אינטרנט (PWA), לטיול סולו של בן משפחה ליפן. המסלול: אוסקה ← קיוטו ← פוג'י ← האקונה ← טוקיו. יציאה 26/9/2026.

## 2. למי זה מיועד
מטייל יחיד בשטח, ביפן, בלי גיבוי צוות. תוכן שגוי (מסעדה סגורה, כתובת שגויה) הוא לא באג קוסמטי - הוא פגיעה ממשית באמון במדריך בזמן אמת.

## 3. Scope - מה בפנים
1. **דף בית (מסלול):** חמש הערים בסדר המסלול, קישור לכל אחת.
2. **דף עיר:** מידע כללי (פסקה), מסעדות/קפה, אטרקציות, מקומות יפים, שווקים, דברים מיוחדים, אזהרות - מאורגן בטאבים.
3. **כרטיס מקום (PlaceCard):** שם עברי + שם לועזי + תיאור + כפתור ניווט ל-Google Maps (בנוי מהשם הלועזי, לא מקואורדינטות) + קישור מידע נוסף אופציונלי. קישורים הם `<a>` רגיל עם `target="_blank"` - **לא** דפוס `window.open` + `setTimeout` (זה דפוס שבאפליקציית המקור גרם לבאג אמיתי במובייל/PWA - נמנע ממנו כאן במכוון מהיום הראשון).
4. **מילון ביטויים:** ביטויי הישרדות בסיסיים ביפנית (רומאג'י) + ניב אזורי לפי עיר היכן שתועד אמת (קנסאי-בן).
5. **רשימת ציוד מודעת-אקלים:** בסיס לכל הטיול (דרכון, מתאם שקע יפני, מזומן וכו') + סעיף לכל עיר במסלול, מבוסס מחקר אקלים אמיתי לסוף ספטמבר-אמצע אוקטובר - **לא** תאריכים ספציפיים לעיר (המשתמש לא יודע את הפירוט היומי, רק את סדר הערים והתאריך הכללי). פוג'י/האקונה (אזור הררי) מוצגים כקרים/גשומים יותר מאוסקה/קיוטו/טוקיו, כי הגובה משפיע על האקלים יותר מהשבוע המדויק.
6. **התקנה כ-PWA, עבודה אופליין מלאה** לאחר ביקור ראשון.

## 4. Scope - מה בחוץ (מודע, לא שכחה)
- **אין תמונות** לאף מקום - לא נבנה pipeline הורדת/אימות תמונות בסשן הזה. `image` תמיד `undefined`.
- **אין back-end, אין חשבון משתמש, אין סנכרון בין מכשירים** - localStorage בלבד לסימון פריטי ציוד.
- **אין תאריכים יומיים לפי עיר** - במכוון, כי אין מידע אמין על כך.

## 5. עקרון איסור המצאה (החוזה המחייב לכל תוכן)
1. כל `Place` חייב `sources: string[]` לא ריק - נאכף בטיפוס.
2. עסק שיכול להיסגר (מסעדה/קפה) - שני מקורות. מקדש/פארק/אתר היסטורי/נוף - מקור אחד מספיק.
3. `coords` לעולם לא מומצא - או שהוא מגיע ממקור מפה אמיתי, או שהשדה לא קיים כלל (ברירת המחדל בפועל: לא קיים באף רשומה).
4. "לא הצלחתי לאמת" עדיף על המצאה למילוי מכסה - הפער מדווח בכנות בדוח המסירה, לא מוסתר.
5. אין אימוג'י, אין דירוגי כוכבים, אין מחירים כמספר קשיח, אין סופרלטיבים בלי עוגן עובדתי, אין "פתוח עכשיו".
6. אין רמז ל"עונת השיא של פריחת הסתיו" (koyo) בחלון הטיול הזה אלא אם אומת ספציפית - ברוב יפן זה נובמבר, אחרי הטיול.

## 6. קריטריוני קבלה
- `npm run build` ו-`npm run lint` עוברים נקי.
- כל חמש הערים נטענות עם תוכן אמיתי (לא stub).
- ניווט/מידע נוסף בכרטיסי מקום נפתחים בטאב חדש בלחיצה רגילה (לא דחוי).
- רשימת הציוד: קופסאות מסומנות נשמרות ב-localStorage, אנימציית pop, קריאה נכונה גם ב-375px.
- אתר חי ב-GitHub Pages, workflow ה-deploy ירוק.

## 7. מכסת תוכן לעיר (זהה בעקרון למקור, מותאם ליפן)
| שדה | כמות יעד | הערה |
|---|---|---|
| מידע כללי | פסקה אחת, 70-120 מילה | איך בנויה / מה מיוחד / מקור השם |
| מסעדות/קפה | 9-10 (אוסקה/קיוטו/טוקיו), 5-8 (פוג'י/האקונה) | לפחות חצי צריכות להיות קפה/ארוחת בוקר |
| אטרקציות | 3-4 (2-4 בפוג'י/האקונה) | |
| מקומות יפים | 2-3 | |
| שווקים | 0-3 | 0 היא תשובה לגיטימית באזורים קטנים |
| מיוחדים | 1-3 | |
| אזהרות | 2-4 | |
| ניב מקומי | 0-2 | רק היכן שמתועד אמת (קנסאי-בן) |

פוג'י והאקונה קטנות יותר במסלול האמיתי - מותר וצפוי שהמכסה שם תהיה נמוכה יותר, זה מדווח בכנות ולא מוסתר.
